# API 地址配置说明

## 可用配置项

### VITE_API_BASE_URL
后端 API 基础地址，用于前端调用后端接口。

## 配置示例

### 开发环境（本地）
```
VITE_API_BASE_URL=http://127.0.0.1:8000/api
```

### 内网访问
```
VITE_API_BASE_URL=http://172.16.7.121:8000/api
```

### 生产环境
```
VITE_API_BASE_URL=https://your-domain.com/api
```

## 使用方法

1. 复制 `.env.example` 为 `.env`
2. 根据需要修改 `VITE_API_BASE_URL`
3. 重启前端开发服务器（配置只在启动时加载）

## 注意事项

- 配置文件只在 Vite 启动时加载，修改后需要重启
- 使用 `VITE_` 前缀的变量才会暴露到客户端代码
- 不要将 `.env` 文件提交到版本控制系统（建议添加到 .gitignore）
